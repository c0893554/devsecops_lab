# Use the official WordPress image as the base
FROM wordpress:latest

# Copy any custom configurations or files (if needed)
# COPY custom-php.ini /usr/local/etc/php/

# Expose the default WordPress port
EXPOSE 80